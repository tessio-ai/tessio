# Optional: override the API base path (defaults to /api/v1, served via the Vite dev proxy).
# VITE_API_BASE_URL=/api/v1
#
# Auth: the SPA no longer needs an org id — sign in and the server derives your
# org + role from the session cookie. Seed a first admin with
# `pnpm --filter @tessio/db db:seed` (see the root README "Authentication" section).
